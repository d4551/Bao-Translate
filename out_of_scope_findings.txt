
================================================================================
FINISH PASS 16 — 2026-06-12 — ROBOTIC VOICES regression: root-caused + fixed (tau=0.0 -> upstream 0.3)
================================================================================
RESEARCH_USED:
- topic: OpenVoice ToneColorConverter tau semantics + upstream default
- source/tool: context7 (/myshell-ai/openvoice demos: convert() called WITHOUT tau) + upstream source
  (openvoice/api.py:141 `def convert(..., tau=0.3, ...)`; openvoice/models.py:220
  `z = (m + randn*tau*exp(logs))` — tau scales posterior SAMPLING NOISE)
- decision affected: TAU constant in OpenVoiceVoiceConverter

DIAGNOSIS: I set TAU=0.0 (FINISH PASS ~10) chasing test determinism. At tau=0 the flow decoder vocodes
the bare posterior MEAN — zero stochastic micro-variation — which in VITS-family models produces
over-smoothed, buzzy, ROBOTIC speech. The sampled noise IS the naturalness. Whisper transcribes robotic
speech fine, so the intelligibility/cosine tests never caught it: overfitting to the metric.
Audited the rest of the audible chain — clean: windowed-sinc anti-aliased resampler, 50ms fades,
shape-preserving peak normalization (linear gain cannot alter timbre), Kokoro/Supertonic untouched,
SCO auto-route only fires on explicit BT-mic selection (no startup restore path).

FIX: TAU = 0.3f (upstream default), comment cites api.py/models.py lines. Test comments updated: clone
assertions are tau-INDEPENDENT invariants (cosine margin >0.05, MAJORITY content-word retention) — never
bitwise determinism. The fixed bundled source (the real flakiness fix) is retained.

VERIFY (no live device access — device run pending):
- compile all 3 source sets + unit suite: BUILD SUCCESSFUL.
- grep: no tau=0 references remain outside historical notes.
- PENDING on-device: BaoTranslateOpenVoiceCloneE2eTest (assertions are majority-threshold, designed to
  absorb tau=0.3's mild variation) + a human listen for naturalness.

OOS (logged, not fixed here): 15 pre-existing deprecation warnings now visible — platform Locale(lang,
country) ctor (use Locale.forLanguageTag/Builder), androidx bundleOf, OssLicensesMenuActivity, and
compose-ui-test v1 createAndroidComposeRule/createComposeRule/createEmptyComposeRule deprecated in favor
of junit4.v2 (StandardTestDispatcher) — a coordinated test-infra migration, separate task.
